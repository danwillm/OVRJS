type HmdQuaternion_t = {
  w: number;
  x: number;
  y: number;
  z: number;
}

type HmdVector3_t = {
  v: [number, number, number];
}

type HmdMatrix34_t = {
  m: [[number, number, number, number], [number, number, number, number], [number, number, number, number]];
}